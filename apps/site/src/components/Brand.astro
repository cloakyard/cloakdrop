---
import { site } from '../data/site';

/** Logo lockup: the scalable SVG mark + wordmark. Used in the header and footer. */
interface Props {
  size?: number;
  class?: string;
}
const { size = 30, class: className } = Astro.props;
const wordmark = site.name.match(/^(.+?[a-z])([A-Z].*)$/);
const nameLead = wordmark?.[1] ?? site.name;
const nameAccent = wordmark?.[2] ?? '';
---

<a class:list={['brand', className]} href="/" aria-label={`${site.name} home`}>
  <img
    class="brand-mark"
    src="/cloakdrop-mark.svg"
    width={size}
    height={size}
    alt=""
    loading="eager"
    decoding="async"
  />
  <span class="brand-name">{nameLead}<span class="brand-accent">{nameAccent}</span></span>
</a>

<style>
  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
  }

  .brand-mark {
    border-radius: 50%;
    /* Whisper of a shadow only — the mark carries its own shared family lighting. */
    filter: drop-shadow(0 1px 3px rgba(15, 42, 60, 0.14));
  }

  .brand-name {
    font-weight: 800;
    letter-spacing: -0.02em;
    color: var(--ink);
    font-size: 1.125rem;
  }

  .brand-accent {
    color: var(--accent-ink);
  }
</style>
